{
  "name": "wow-js-jail",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "build": "esbuild index.ts --bundle --platform=node --format=cjs --packages=bundle --outfile=dist/index.cjs && esbuild sandbox.ts --bundle --platform=node --format=esm --packages=bundle --outfile=dist/sandbox.mjs && sed -i 's|\"./sandbox.ts\"|\"./sandbox.mjs\"|g' dist/index.cjs && cp -r public dist/public",
    "preview": "pnpm run build && cd dist && FLAG=${FLAG:-HZU18{test_flag}} node index.cjs"
  },
  "keywords": [],
  "author": "Zolboobayar Gantumur <zolboobayar.gantumur@owasp.org>",
  "license": "MIT",
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.7",
    "esbuild": "^0.27.4",
    "vitest": "^4.0.17"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
